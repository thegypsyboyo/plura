const AdminLayout = ({ children }: {children:React.ReactNode}) => {
  return <div className="flex items-center justify-center bg-blue-600 min-h-screen">{children}</div>;
};

export default AdminLayout;
